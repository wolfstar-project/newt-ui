## Summary

<!-- What changed? Keep this focused on observable behavior. -->

## Why

<!-- What problem does this solve? -->

## Verification

<!-- List exact commands, tests, or manual scenarios and their results. -->

- [ ] `pnpm format:check`
- [ ] `pnpm lint`
- [ ] `pnpm knip`
- [ ] `pnpm typecheck`
- [ ] `pnpm build`

## Safety and compatibility

- [ ] Does this change affect the `--newt-*` design tokens or the CLI's
      `components.json` schema? If so, note the compatibility impact.
- [ ] Did you add a changeset (`pnpm changeset`) if this touches a
      published package (`packages/*`)?
