name: Automatic Label Sync

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions: {}

jobs:
  label_sync:
    permissions:
      contents: read # reusable workflow checks out this repo for the overwrite labels
      issues: write # create and update repository labels
    uses: wolfstar-project/.github/.github/workflows/reusable-labelsync.yml@fb3b0e52ddb109fec8a64f26a9295c9e72528dc8
    with:
      merge-labels: true
      repository-overwrite-labels: wolfstar-project/newt-ui
