diff --git a/src/lib/i18n.ts b/src/lib/i18n.ts
index 3bef74816d8f5bf10237c189b6dec92a78e639d9..ba502446d10ccdb9ad36ae8a46533a50280061db 100644
--- a/src/lib/i18n.ts
+++ b/src/lib/i18n.ts
@@ -179,7 +179,8 @@ export function getLocalizedHref(
   ].filter(Boolean).join('/');
   const basePath = config.docsBase === '/' ? '' : config.docsBase.replace(/\/$/, '');
 
-  return routeSlug ? `${basePath}/${routeSlug}/` : `${basePath || '/'}`;
+  // Patched: the site runs with `trailingSlash: "never"`.
+  return routeSlug ? `${basePath}/${routeSlug}` : `${basePath || '/'}`;
 }
 
 export function getLocalizedMarkdownHref(
diff --git a/src/lib/routes.ts b/src/lib/routes.ts
index f47a6bf703192461dfd20a1b0dcf65caa250d7be..57b4b6d6d38b3b4ce57ad1d23bd8a30a6842af23 100644
--- a/src/lib/routes.ts
+++ b/src/lib/routes.ts
@@ -28,20 +28,8 @@ function hasLocalizedRoutes(config: LotusThemeConfig): boolean {
 export function getLotusInjectedRoutes(config: LotusThemeConfig): LotusInjectedRoute[] {
   const docsBasePath = normalizeDocsBasePath(config.docsBase);
   const llmsConfig = resolveLlmsConfig(config);
-  const routes: LotusInjectedRoute[] = [
-    {
-      pattern: '/404',
-      entrypoint: new URL('../routes/404.astro', import.meta.url),
-    },
-    {
-      pattern: getDocsRoutePattern(docsBasePath, '/[...slug]'),
-      entrypoint: new URL('../routes/docs.astro', import.meta.url),
-    },
-    {
-      pattern: getDocsRoutePattern(docsBasePath, '/[...slug].md'),
-      entrypoint: new URL('../routes/docs.md.ts', import.meta.url),
-    },
-  ];
+  // Patched: the site owns /404, /docs/[...slug] and /docs/[...slug].md.
+  const routes: LotusInjectedRoute[] = [];
 
   if (llmsConfig.enabled) {
     routes.push({
diff --git a/src/lib/search/pagefind.ts b/src/lib/search/pagefind.ts
index 126cf3a3d42834b3bc9be757f80cf7cce24cc041..e81e26d04bf2bd5ebb1e0b62396a4feb87c78f81 100644
--- a/src/lib/search/pagefind.ts
+++ b/src/lib/search/pagefind.ts
@@ -19,7 +19,19 @@ interface PagefindModule {
 
 type PagefindLoader = () => Promise<PagefindModule>;
 
-export async function loadPagefind(importPagefind: PagefindLoader = () => import('pagefind')): Promise<PagefindModule> {
+/*
+ * Patched: this file is executed by Vite's SSR module runner, which Astro
+ * closes before `astro:build:done`. A plain `import('pagefind')` is rewritten
+ * to a runner call and fails with "Vite module runner has been closed", so the
+ * import is handed to the host's own loader instead.
+ */
+const nativeImport = new Function('specifier', 'return import(specifier)') as (
+  specifier: string,
+) => Promise<unknown>;
+
+export async function loadPagefind(
+  importPagefind: PagefindLoader = () => nativeImport('pagefind') as Promise<PagefindModule>,
+): Promise<PagefindModule> {
   try {
     return await importPagefind();
   } catch (error) {
