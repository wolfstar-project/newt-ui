---
title: Tabs
description: A pill-style tab list that switches the active tab on click.
---

<ComponentPreview name="tabs-demo" />

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">```bash npx newt-ui@latest add tabs ```</TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="tabs" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { Tabs, TabsTrigger, TabsContent } from "@/components/ui/tabs"
```

```tsx
<Tabs defaultValue="one">
  <TabsTrigger value="one">Tab one</TabsTrigger>
  <TabsTrigger value="two">Tab two</TabsTrigger>
  <TabsTrigger value="three">Tab three</TabsTrigger>
</Tabs>
```

## Examples

### Controlled

```tsx
const [tab, setTab] = React.useState("one")

<Tabs value={tab} onValueChange={setTab}>
  <TabsTrigger value="one">Tab one</TabsTrigger>
  <TabsTrigger value="two">Tab two</TabsTrigger>
</Tabs>
```

### With content panels

```tsx
<Tabs defaultValue="overview">
  <TabsTrigger value="overview">Overview</TabsTrigger>
  <TabsTrigger value="members">Members</TabsTrigger>
</Tabs>
```

Render `TabsContent` anywhere inside the same `Tabs` tree to show a panel for the active value.
