---
title: Slash Command Tag
description: Inline monospace tag for referencing a /slash command in text.
---

<ComponentPreview name="slash-command-demo" />

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash npx newt-ui@latest add slash-command ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="slash-command" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import { SlashCommand } from "@/components/ui/slash-command"
```

```tsx
<p>
  Run <SlashCommand command="deploy" /> to ship the latest build.
</p>
```
