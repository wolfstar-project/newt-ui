---
title: Bot Command Card
description: A card documenting a slash command, its owning bot and its typed options.
---

<ComponentPreview name="bot-command-card-demo" />

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash npx @wolfstar/newt-ui@latest add bot-command-card ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="bot-command-card" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import {
  BotCommandCard,
  BotCommandCardDescription,
  BotCommandCardHeader,
  BotCommandCardName,
  BotCommandCardOption,
  BotCommandCardOptions,
} from "@/components/ui/bot-command-card"
```

```tsx
<BotCommandCard>
  <BotCommandCardHeader>
    <BotCommandCardName>ban</BotCommandCardName>
    <Badge variant="brand">mod-bot</Badge>
  </BotCommandCardHeader>
  <BotCommandCardDescription>
    Ban a member from the server.
  </BotCommandCardDescription>
  <BotCommandCardOptions>
    <BotCommandCardOption
      name="user"
      type="USER"
      required
      description="Member to ban"
    />
    <BotCommandCardOption
      name="reason"
      type="STRING"
      description="Audit log reason"
    />
  </BotCommandCardOptions>
</BotCommandCard>
```

## Examples

### Without the leading slash

```tsx
<BotCommandCardName slash={false}>context-menu action</BotCommandCardName>
```
