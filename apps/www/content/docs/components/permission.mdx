---
title: Permission Row
description: A settings row with a deny / inherit / allow tri-state toggle.
---

<ComponentPreview name="permission-demo" />

## Installation

<Tabs defaultValue="cli">
  <TabsList>
    <TabsTrigger value="cli">CLI</TabsTrigger>
    <TabsTrigger value="manual">Manual</TabsTrigger>
  </TabsList>
  <TabsContent value="cli">
    ```bash npx @newtui/react@latest add permission ```
  </TabsContent>
  <TabsContent value="manual">
    <Steps>
      <Step>Copy and paste the following code into your project.</Step>
      <ComponentSource name="permission" />
      <Step>Update the import paths to match your project setup.</Step>
    </Steps>
  </TabsContent>
</Tabs>

## Usage

```tsx
import {
  Permission,
  PermissionDescription,
  PermissionInfo,
  PermissionName,
  PermissionStates,
} from "@/components/ui/permission"
```

```tsx
<Permission>
  <PermissionInfo>
    <PermissionName>Manage Channels</PermissionName>
    <PermissionDescription>
      Create, edit and delete channels.
    </PermissionDescription>
  </PermissionInfo>
  <PermissionStates aria-label="Manage Channels" defaultValue="inherit" />
</Permission>
```

## Examples

### Controlled

```tsx
const [value, setValue] = React.useState<PermissionValue>("allow")

<PermissionStates value={value} onValueChange={setValue} aria-label="Send Messages" />
```

Stack several `Permission` rows inside a container; every row except the last draws a bottom border.
