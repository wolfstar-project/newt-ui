---
title: "Media Gallery"
description: "One to ten images in a mosaic, laid out by how many there are."
component: true
---

One to ten images in a mosaic, laid out by how many there are. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="media-gallery" />

## Installation

<Installation name="media-gallery" />

## Usage

<Usage name="media-gallery" />

## API reference

<PropsTable
  of="MediaGallery"
  rows={[
    {
      name: "children / default slot",
      type: "React.ReactNode / Vue slot",
      description: "Content rendered inside the component.",
    },
    {
      name: "className / class",
      type: "string",
      description: "Additional classes merged with the component defaults.",
    },
  ]}
/>

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="media-gallery" />
