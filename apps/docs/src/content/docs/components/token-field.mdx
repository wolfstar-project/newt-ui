---
title: "Token Field"
description: "A read-only, masked monospace input with reveal and copy-to-clipboard actions for secrets such as bot tokens."
component: true
---

A read-only, masked monospace input with reveal and copy-to-clipboard actions for secrets such as bot tokens. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="token-field" />

## Installation

<Installation name="token-field" />

## Usage

<Usage name="token-field" />

## API reference

<PropsTable
  of="TokenField"
  rows={[
    {
      name: "value",
      type: "string",
      description: "The secret value. Masked until revealed.",
    },
    {
      name: "id",
      type: "string | undefined",
      description: "Id forwarded to the input so a label can target it.",
    },
    {
      name: "name",
      type: "string | undefined",
      description:
        "Accessible name for the input when no visible label is linked.",
    },
    {
      name: "revealLabel",
      type: "string | undefined",
      description: "Label of the reveal button while hidden.",
    },
    {
      name: "hideLabel",
      type: "string | undefined",
      description: "Label of the reveal button while shown.",
    },
    {
      name: "copyLabel",
      type: "string | undefined",
      description: "Label of the copy button.",
    },
    {
      name: "copiedLabel",
      type: "string | undefined",
      description: "Label of the copy button right after copying.",
    },
    {
      name: "copiedDuration",
      type: "number | undefined",
      description: "How long (ms) the copied label is shown.",
    },
    {
      name: "onCopy",
      type: "() => void | undefined",
      description: "Called after the value is written to the clipboard.",
    },
  ]}
/>

The registry exports `TokenField`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Label the token input, announce copy feedback through a polite live region, and never make the reveal or copy actions icon-only without an accessible name.

## Tokens

<TokensNote name="token-field" />
