---
title: "Custom Emoji"
description: "An uploaded emoji that sits on the text baseline and reads as :name:."
component: true
---

An uploaded emoji that sits on the text baseline and reads as :name:. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="custom-emoji" />

## Installation

<Installation name="custom-emoji" />

## Usage

<Usage name="custom-emoji" />

## API reference

<PropsTable
  of="CustomEmoji"
  rows={[
    {
      name: "children / default slot",
      type: "React.ReactNode / Vue slot",
      description: "Content rendered inside the component.",
    },
    {
      name: "className / class",
      type: "string",
      description: "Additional classes merged with the component defaults.",
    },
  ]}
/>

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="custom-emoji" />
