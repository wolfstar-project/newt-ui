---
title: "Progress Bar"
description: "A gradient progress bar for boosts, uploads and other long-running tasks."
component: true
---

A gradient progress bar for boosts, uploads and other long-running tasks. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="progress" />

## Installation

<Installation name="progress" />

## Usage

<Usage name="progress" />

## API reference

<PropsTable
  of="Progress"
  rows={[
    {
      name: "value",
      type: "number | undefined",
      description: "Current value, 0–max",
    },
    {
      name: "max",
      type: "number | undefined",
      description: "Optional max value.",
    },
    {
      name: "indicatorClassName",
      type: "string | undefined",
      description: "Optional indicatorClassName value.",
    },
  ]}
/>

The registry exports `Progress`, `ProgressLabel`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="progress" />
