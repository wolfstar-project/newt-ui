---
title: "Attachment Card"
description: "File card showing a type icon, file name, size and a download action."
component: true
---

File card showing a type icon, file name, size and a download action. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="attachment" />

## Installation

<Installation name="attachment" />

## Usage

<Usage name="attachment" />

## API reference

<PropsTable
  of="Attachment"
  rows={[
    {
      name: "children / default slot",
      type: "React.ReactNode / Vue slot",
      description: "Content rendered inside the component.",
    },
    {
      name: "className / class",
      type: "string",
      description: "Additional classes merged with the component defaults.",
    },
  ]}
/>

The registry exports `Attachment`, `AttachmentIcon`, `AttachmentMeta`, `AttachmentName`, `AttachmentSize`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="attachment" />
