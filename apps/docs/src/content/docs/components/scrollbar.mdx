---
title: "Scrollbar Utility"
description: "Discord-styled scrollbars for any scrollable container, with an optional keyboard-focusable viewport."
component: true
---

Discord-styled scrollbars for any scrollable container, with an optional keyboard-focusable viewport. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="scrollbar" />

## Installation

<Installation name="scrollbar" />

## Usage

<Usage name="scrollbar" />

## API reference

<PropsTable
  of="ScrollArea"
  rows={[
    {
      name: "focusable",
      type: "boolean | undefined",
      description:
        "Put the viewport in the tab order so it can be scrolled from the keyboard (WCAG 2.1.1). Off by default: a focusable region only helps when the area actually scrolls and holds no other focusable content.",
    },
    {
      name: "viewportLabel",
      type: "string | undefined",
      description: "Accessible name of the focusable viewport.",
    },
  ]}
/>

The registry exports `ScrollArea`, `Scrollbar`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="scrollbar" />
