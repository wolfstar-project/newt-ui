---
title: "Member List"
description: "A server member sidebar: role sections, presence, coloured names and APP tags."
component: true
---

A server member sidebar: role sections, presence, coloured names and APP tags. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="member-list" />

## Installation

<Installation name="member-list" />

## Usage

<Usage name="member-list" />

## API reference

<PropsTable
  of="MemberListSection"
  rows={[
    {
      name: "offline",
      type: "boolean | undefined",
      description: "Optional offline value.",
    },
  ]}
/>

<PropsTable
  of="MemberListName"
  rows={[
    {
      name: "colored",
      type: "boolean | undefined",
      description:
        "Render the name in the brand colour (e.g. highlighted role).",
    },
    {
      name: "color",
      type: "string | undefined",
      description: "Role colour. Any CSS colour; wins over `colored`.",
    },
  ]}
/>

<PropsTable
  of="MemberListAppTag"
  rows={[
    {
      name: "verified",
      type: "boolean | undefined",
      description: "Optional verified value.",
    },
  ]}
/>

The registry exports `MemberList`, `MemberListSection`, `MemberListHeading`, `MemberListItem`, `MemberListInfo`, `MemberListNameRow`, `MemberListName`, `MemberListAppTag`, `MemberListRole`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="member-list" />
