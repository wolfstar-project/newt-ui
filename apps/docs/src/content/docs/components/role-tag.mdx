---
title: "Role Tag"
description: "A compact bordered tag with a coloured dot for displaying a member's role."
component: true
---

A compact bordered tag with a coloured dot for displaying a member's role. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="role-tag" />

## Installation

<Installation name="role-tag" />

## Usage

<Usage name="role-tag" />

## API reference

<PropsTable
  of="RoleTag"
  rows={[
    {
      name: "color",
      type: "string | undefined",
      description:
        "Role colour applied to the tag; the dot inherits it via `currentColor`.",
    },
  ]}
/>

The registry exports `RoleTag`, `RoleTagDot`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="role-tag" />
