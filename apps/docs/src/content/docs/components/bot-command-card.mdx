---
title: "Bot Command Card"
description: "A card documenting a slash command, its owning bot and its typed options."
component: true
---

A card documenting a slash command, its owning bot and its typed options. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="bot-command-card" />

## Installation

<Installation name="bot-command-card" />

## Usage

<Usage name="bot-command-card" />

## API reference

<PropsTable
  of="BotCommandCardName"
  rows={[
    {
      name: "slash",
      type: "boolean | undefined",
      description: "Render the muted leading slash. Defaults to true.",
    },
  ]}
/>

<PropsTable
  of="BotCommandCardOption"
  rows={[
    {
      name: "name",
      type: "string",
      description: "Option name, rendered in monospace.",
    },
    {
      name: "type",
      type: "string | undefined",
      description: "Option type, e.g. `STRING`, `USER`, `INTEGER`.",
    },
    {
      name: "required",
      type: "boolean | undefined",
      description: "Marks the option as required.",
    },
    {
      name: "requiredLabel",
      type: "string | undefined",
      description: "Label used for the required marker.",
    },
    {
      name: "description",
      type: "React.ReactNode | undefined",
      description: "Human readable description.",
    },
  ]}
/>

The registry exports `BotCommandCard`, `BotCommandCardHeader`, `BotCommandCardName`, `BotCommandCardDescription`, `BotCommandCardOptions`, `BotCommandCardOption`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="bot-command-card" />
