---
title: "Embed"
description: "A Discord-inspired rich message embed with an accent bar, author, title, description, fields, footer and timestamp."
component: true
---

A Discord-inspired rich message embed with an accent bar, author, title, description, fields, footer and timestamp. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="embed" />

## Installation

<Installation name="embed" />

## Usage

<Usage name="embed" />

## API reference

<PropsTable
  of="Embed"
  rows={[
    {
      name: "color",
      type: "string | undefined",
      description:
        "Accent colour of the left bar. Any CSS colour; defaults to the brand.",
    },
  ]}
/>

<PropsTable
  of="EmbedTimestamp"
  rows={[
    {
      name: "date",
      type: "Date | number | string",
      description: "The moment to render. Anything `new Date()` accepts.",
    },
  ]}
/>

The registry exports `Embed`, `EmbedAuthor`, `EmbedAuthorIcon`, `EmbedAuthorName`, `EmbedEyebrow`, `EmbedTitle`, `EmbedDescription`, `EmbedFields`, `EmbedField`, `EmbedFieldName`, `EmbedFieldValue`, `EmbedFooter`, `EmbedFooterIcon`, `EmbedFooterSeparator`, `EmbedTimestamp`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="embed" />
