---
title: "Chat window"
description: "A whole channel view: header, scrolling message list and composer, composed from the registry."
component: true
---

A whole channel view: header, scrolling message list and composer, composed from the registry. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="chat-window" />

## Installation

<Installation name="chat-window" />

## Usage

<Usage name="chat-window" />

## API reference

<PropsTable
  of="ChatWindow"
  rows={[
    {
      name: "channelName",
      type: "string",
      description: "Required channelName value.",
    },
    {
      name: "topic",
      type: "string | undefined",
      description: "Optional topic value.",
    },
    {
      name: "messages",
      type: "readonly ChatMessage[]",
      description: "Required messages value.",
    },
    {
      name: "onSend",
      type: "(message: string) => void | undefined",
      description: "Optional onSend value.",
    },
  ]}
/>

The registry exports `ChatMessage`, `ChatWindow`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="chat-window" />
