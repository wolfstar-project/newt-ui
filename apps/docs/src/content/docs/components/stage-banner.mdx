---
title: "Stage Banner"
description: "A full-width announcement strip with a status dot, optional action button and dismiss control."
component: true
---

A full-width announcement strip with a status dot, optional action button and dismiss control. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="stage-banner" />

## Installation

<Installation name="stage-banner" />

## Usage

<Usage name="stage-banner" />

## API reference

<PropsTable
  of="StageBanner"
  rows={[
    {
      name: "open",
      type: "boolean | undefined",
      description:
        "Controlled visibility; when omitted the banner manages its own state.",
    },
    {
      name: "onOpenChange",
      type: "(open: boolean) => void | undefined",
      description: "Fired when the banner is dismissed via StageBannerClose.",
    },
  ]}
/>

The registry exports `StageBanner`, `StageBannerDot`, `StageBannerAction`, `StageBannerClose`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="stage-banner" />
