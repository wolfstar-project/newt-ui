---
title: "Voice Channel Row"
description: "A sidebar voice channel row with a collapsible header and the list of connected members."
component: true
---

A sidebar voice channel row with a collapsible header and the list of connected members. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="voice-channel" />

## Installation

<Installation name="voice-channel" />

## Usage

<Usage name="voice-channel" />

## API reference

<PropsTable
  of="VoiceChannelMemberIcons"
  rows={[
    {
      name: "muted",
      type: "boolean | undefined",
      description:
        "Renders the icons in the dnd color to signal a muted / deafened member.",
    },
  ]}
/>

The registry exports `VoiceChannel`, `VoiceChannelHeader`, `VoiceChannelIcon`, `VoiceChannelMembers`, `VoiceChannelMember`, `VoiceChannelMemberIcons`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="voice-channel" />
