---
title: "Server Banner"
description: "Invite-style card with a server icon, name, live member counts and a join button."
component: true
---

Invite-style card with a server icon, name, live member counts and a join button. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="server-banner" />

## Installation

<Installation name="server-banner" />

## Usage

<Usage name="server-banner" />

## API reference

<PropsTable
  of="ServerBanner"
  rows={[
    {
      name: "children / default slot",
      type: "React.ReactNode / Vue slot",
      description: "Content rendered inside the component.",
    },
    {
      name: "className / class",
      type: "string",
      description: "Additional classes merged with the component defaults.",
    },
  ]}
/>

The registry exports `ServerBanner`, `ServerBannerIcon`, `ServerBannerInfo`, `ServerBannerName`, `ServerBannerMeta`, `ServerBannerMetaItem`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="server-banner" />
