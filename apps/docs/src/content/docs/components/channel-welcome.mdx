---
title: "Channel Welcome"
description: "The header that opens a channel, shown above its very first message."
component: true
---

The header that opens a channel, shown above its very first message. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="channel-welcome" />

## Installation

<Installation name="channel-welcome" />

## Usage

<Usage name="channel-welcome" />

## API reference

<PropsTable
  of="ChannelWelcomeDate"
  rows={[
    {
      name: "dateTime",
      type: "string | undefined",
      description: "Machine-readable date for the rendered `<time>` element.",
    },
    {
      name: "children",
      type: "React.ReactNode | undefined",
      description: "Optional children value.",
    },
  ]}
/>

The registry exports `ChannelWelcome`, `ChannelWelcomeIcon`, `ChannelWelcomeTitle`, `ChannelWelcomeDescription`, `ChannelWelcomeAction`, `ChannelWelcomeDate`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="channel-welcome" />
