---
title: "Message Group"
description: "A grouped chat message with author, timestamp, reply preview, reactions and an ephemeral state."
component: true
---

A grouped chat message with author, timestamp, reply preview, reactions and an ephemeral state. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="message-group" />

## Installation

<Installation name="message-group" />

## Usage

<Usage name="message-group" />

## API reference

<PropsTable
  of="MessageGroup"
  rows={[
    {
      name: "withReply",
      type: "boolean | undefined",
      description:
        "Lay the row out as a 2x2 grid so a `MessageGroupReply` can hook over the avatar, the way the client stacks a reply above the message it answers.",
    },
    {
      name: "ephemeral",
      type: "boolean | undefined",
      description:
        "Tint the row as an ephemeral (visible-to-you-only) response.",
    },
  ]}
/>

<PropsTable
  of="MessageGroupBotTag"
  rows={[
    {
      name: "verified",
      type: "boolean | undefined",
      description: "Add the check Discord puts on verified applications.",
    },
  ]}
/>

The registry exports `MessageGroup`, `MessageGroupAvatar`, `MessageGroupReply`, `MessageGroupBody`, `MessageGroupHeader`, `MessageGroupAuthor`, `MessageGroupBotTag`, `MessageGroupTime`, `MessageGroupContent`, `MessageGroupReactions`, `MessageGroupEphemeralNotice`, `MessageGroupEphemeralAction`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="message-group" />
