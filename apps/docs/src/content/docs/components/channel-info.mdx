---
title: "Channel Info"
description: "The channel detail panel that covers a channel on narrow screens."
component: true
---

The channel detail panel that covers a channel on narrow screens. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="channel-info" />

## Installation

<Installation name="channel-info" />

## Usage

<Usage name="channel-info" />

## API reference

<PropsTable
  of="ChannelInfo"
  rows={[
    {
      name: "children / default slot",
      type: "React.ReactNode / Vue slot",
      description: "Content rendered inside the component.",
    },
    {
      name: "className / class",
      type: "string",
      description: "Additional classes merged with the component defaults.",
    },
  ]}
/>

The registry exports `ChannelInfo`, `ChannelInfoToolbar`, `ChannelInfoActions`, `ChannelInfoAction`, `ChannelInfoIdentity`, `ChannelInfoIcon`, `ChannelInfoName`, `ChannelInfoType`, `ChannelInfoPanel`, `ChannelInfoEmpty`, `ChannelInfoEmptyIcon`, `ChannelInfoEmptyTitle`, `ChannelInfoEmptyBody`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="channel-info" />
