---
title: "Cooldown Bar"
description: "A slim progress bar with a label and monospace countdown for command cooldowns."
component: true
---

A slim progress bar with a label and monospace countdown for command cooldowns. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="cooldown-bar" />

## Installation

<Installation name="cooldown-bar" />

## Usage

<Usage name="cooldown-bar" />

## API reference

<PropsTable
  of="CooldownBarTrack"
  rows={[
    {
      name: "value",
      type: "number",
      description: "Fill percentage, 0-100.",
    },
    {
      name: "ready",
      type: "boolean | undefined",
      description: 'Switch the fill to the "ready" (online green) colour.',
    },
  ]}
/>

The registry exports `CooldownBar`, `CooldownBarLabel`, `CooldownBarValue`, `CooldownBarTrack`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="cooldown-bar" />
