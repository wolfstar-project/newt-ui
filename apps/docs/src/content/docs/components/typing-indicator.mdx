---
title: "Typing Indicator"
description: "Three bouncing dots with a label that signals someone is typing."
component: true
---

Three bouncing dots with a label that signals someone is typing. Compose it from the exported registry parts so local content and behaviour stay in the application while the visual contract stays token-driven.

<ComponentPreview name="typing-indicator" />

## Installation

<Installation name="typing-indicator" />

## Usage

<Usage name="typing-indicator" />

## API reference

<PropsTable
  of="TypingIndicator"
  rows={[
    {
      name: "label",
      type: "string | undefined",
      description: "Accessible label announced to screen readers.",
    },
  ]}
/>

The registry exports `TypingIndicator`, `TypingIndicatorDots`. Parts without a table accept the native attributes for the element they render; React uses `className`, while Vue uses `class` and fallthrough attributes.

## Accessibility

Preserve the native semantics of the rendered elements and pass through ARIA attributes when the surrounding context needs a label or description. Any interactive child needs a visible focus indicator, and meaning must not rely on colour alone.

## Tokens

<TokensNote name="typing-indicator" />
