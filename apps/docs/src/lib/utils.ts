/*
 * The registry components are copied in with their imports intact, so
 * `@/lib/utils` has to resolve here for a demo to render. It is the same
 * re-export the CLI writes into a consumer's project.
 */
export { cn } from "cn"
