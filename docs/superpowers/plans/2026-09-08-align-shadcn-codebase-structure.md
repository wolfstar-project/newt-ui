# shadcn Codebase Structure Alignment Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Align the newt/ui app registries and CLI package layout with the current shadcn React and Vue repositories while preserving newt/ui's unified docs and CLI architecture.

**Architecture:** Registry source lives at each application root under `registry/bases/newt`, with generated loaders in root-level `__registry__`. The published `newtui` implementation moves to `packages/cli` and adopts the upstream CLI responsibility folders; compatibility wrappers move under `deprecated/` and keep their published names.

**Tech Stack:** pnpm 11, Turborepo, TypeScript, Next.js, Nuxt 4, Astro, React, Vue 3, Vitest, Oxc

**Spec:** `docs/superpowers/specs/2026-09-08-align-shadcn-codebase-structure.md`

## Global Constraints

- Keep package names, executable names, registry URLs, and `components.json` compatibility stable.
- Keep one shared Astro docs application and one multi-framework CLI.
- Keep React on Tailwind 3 and Vue on Tailwind 4 as compatibility fixtures.
- Keep generated registry JSON paths valid for the CLI's import transformer.
- Preserve all component source and metadata while moving directories.
- Update contributor documentation and local skills to the canonical paths.

---

### Task 1: Align application registry ownership

**Files:**

- Move: `apps/www/registry/default` to `apps/www/registry/bases/newt`
- Move: `apps/vue/app/lib/registry` to `apps/vue/registry`
- Move: `apps/vue/app/__registry__` to `apps/vue/__registry__`
- Modify: `scripts/gen-registry.mjs`
- Modify: application configs, build scripts, imports, tests, and docs aliases that reference the old paths

**Interfaces:**

- Consumes: component metadata from `apps/www/registry/meta/*.json`
- Produces: `registryUi`, `registryExamples`, root `registry.json`, and generated `__registry__` loaders for both apps

- [ ] **Step 1: Add a structure assertion to the registry test**

Assert that both frameworks expose `registry/bases/newt/{ui,blocks,examples}` and that Vue's registry is outside `app/`.

- [ ] **Step 2: Run the focused test and confirm the old layout fails**

Run: `pnpm --filter newtui test -- registry.test.ts`

- [ ] **Step 3: Move registry sources and update the generator**

Use `bases/newt`, `blocks`, and `examples` consistently in source imports and generated file paths. Resolve Vue registry imports from the Nuxt root with `~~/registry/...` because `@` continues to point at `app/`.

- [ ] **Step 4: Regenerate and verify both registries**

Run: `node scripts/gen-registry.mjs && pnpm registry:build`

- [ ] **Step 5: Checkpoint**

Review `git diff --stat` and ensure no component or metadata file was lost.

### Task 2: Make `packages/cli` the canonical CLI package

**Files:**

- Move: `packages/newtui` to `packages/cli`
- Move: former `packages/cli` wrapper to `deprecated/vue-cli`
- Move: `packages/newt-ui` wrapper to `deprecated/react-cli`
- Modify: `pnpm-workspace.yaml`, package manifests, lockfile, Changesets config, Knip config, scripts, and documentation

**Interfaces:**

- Consumes: registry JSON generated by `scripts/gen-registry.mjs`
- Produces: the unchanged `newtui` and `newtui-html` executables plus deprecated forwarding packages

- [ ] **Step 1: Extend the workspace package patterns for deprecated wrappers**

Add `deprecated/*` and retain `packages/*`.

- [ ] **Step 2: Move packages without changing their published identities**

Update repository directory metadata and relative Oxc configuration paths. Keep dependencies on `newtui` as `workspace:*`.

- [ ] **Step 3: Refresh workspace links and lockfile importers**

Run: `pnpm install --lockfile-only`

- [ ] **Step 4: Verify CLI binaries and wrappers**

Run: `pnpm --filter newtui build && pnpm --filter @newtui/react lint && pnpm --filter @newtui/vue lint`

- [ ] **Step 5: Checkpoint**

Confirm `pnpm --filter newtui exec node dist/index.js --help` exits successfully.

### Task 3: Align CLI internal responsibility folders

**Files:**

- Move: `packages/cli/src/tools/config.ts` to `packages/cli/src/utils/get-config.ts`
- Move: `packages/cli/src/tools/schema.ts` to `packages/cli/src/registry/schema.ts`
- Move: `packages/cli/src/tools/registry.ts` to `packages/cli/src/registry/api.ts`
- Move: `packages/cli/src/tools/transformers.ts` to `packages/cli/src/utils/transformers/imports.ts`
- Move: remaining `packages/cli/src/tools/*.ts` to `packages/cli/src/utils/*.ts`
- Move: `packages/cli/test/*.test.ts` beside the implementation it exercises
- Create: `packages/cli/src/preflights/.gitkeep`
- Create: `packages/cli/src/utils/updaters/.gitkeep`
- Modify: all affected imports and Vitest include configuration

**Interfaces:**

- Consumes: `Config`, `Framework`, `RegistryItem`, and registry API functions
- Produces: the same public CLI behavior from responsibility-scoped modules

- [ ] **Step 1: Move schema and registry boundary modules**

Update imports so commands consume schemas from `schema/` and network registry operations from `registry/`.

- [ ] **Step 2: Move utilities and transformer modules**

Keep implementation behavior unchanged and adjust relative `.js` import specifiers.

- [ ] **Step 3: Colocate tests**

Place each test with the narrowest owning module; keep the cross-registry logical-property test under `src/registry/`.

- [ ] **Step 4: Run CLI tests and typecheck**

Run: `pnpm --filter newtui test && pnpm --filter newtui typecheck`

- [ ] **Step 5: Checkpoint**

Run: `rg 'src/tools|../tools|./tools' packages/cli` and expect no matches.

### Task 4: Synchronize documentation and repository automation

**Files:**

- Modify: `AGENTS.md`, `AGENT_GUIDE.md`, `README.md`, `CONTRIBUTING.md`
- Modify: `.skills/newt-ui-{architecture,registry,cli,components}/SKILL.md`
- Modify: docs content containing contributor source paths
- Modify: comments in templates and application styles

**Interfaces:**

- Consumes: canonical paths established by Tasks 1–3
- Produces: contributor instructions with no stale source path references

- [ ] **Step 1: Replace old source paths in contributor documentation**

Document `apps/*/registry/bases/newt`, `packages/cli`, and `deprecated/*` as the only canonical locations.

- [ ] **Step 2: Update generated examples and explanatory snippets**

Keep consumer-facing installed aliases such as `@/components/ui` unchanged; only registry-source examples change.

- [ ] **Step 3: Scan for stale paths**

Run: `rg 'packages/newtui|packages/newt-ui|apps/vue/app/lib/registry|registry/default|/default/(ui|block|example)' --glob '!pnpm-lock.yaml'` and inspect every remaining match.

- [ ] **Step 4: Verify skill symlinks remain valid**

Run: `find .agents/skills .claude/skills -xtype l` and expect no output.

- [ ] **Step 5: Checkpoint**

Review the documentation diff against the target tree in the spec.

### Task 5: Full repository verification

**Files:**

- Modify: only files required by failures caused by this migration

**Interfaces:**

- Consumes: all migrated application and CLI paths
- Produces: a buildable, lint-clean monorepo with reproducible generated registries

- [ ] **Step 1: Verify formatting and lint**

Run: `pnpm format:check && pnpm lint`

- [ ] **Step 2: Verify types and tests**

Run: `pnpm typecheck && pnpm test`

- [ ] **Step 3: Verify production builds and unused-code analysis**

Run: `pnpm build && pnpm knip`

- [ ] **Step 4: Verify generated outputs are stable**

Run `node scripts/gen-registry.mjs` again and confirm `git diff --exit-code` reports no generator-only drift beyond the intentional migration.

- [ ] **Step 5: Final review**

Compare the resulting tree with both upstream layouts and record intentional newt/ui deviations in the spec and final handoff.
